[application]
name = "sirin"
default_platform = "web"

[web.app]
title = "Sirin — AI Agent Platform"

[web.watcher]
reload_html = true
watch_path = ["src"]

[web.resource]
# Tailwind CDN for development (replaced by bundled CSS in production)
style = []
script = []

[web.resource.dev]
style = []
script = []
