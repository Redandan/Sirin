{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "default",
  "description": "Default capability – grants the main window access to invoke Rust commands and receive events.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
